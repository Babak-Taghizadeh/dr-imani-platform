declare module "@majidh1/jalalidatepicker/dist/jalalidatepicker.min.js" {
  // This module loads the jalaliDatepicker library and makes it available on window
  // The actual implementation is a UMD module that sets window.jalaliDatepicker
}
